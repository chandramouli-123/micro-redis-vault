{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "PORT=${PORT:-8080} python3 micro_redis_vault.py --web --web-port ${PORT:-8080}"
  }
}
